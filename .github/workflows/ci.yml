# .github/workflows/ci.yml
name: Tiny workflow

on:
  pull_request:

jobs:
  release-comment:
    permissions: write-all
    runs-on: ubuntu-latest
    steps:
      -
        name: Checkout
        uses: actions/checkout@v2
      -
        name: Run release-confirmation
        uses: tosite/release-confirmation-action@v0.0.31
        with:
          action: released
          isDebug: 1
